version: '3.9'
services:
  hflow3:
    build: .
    environment:
      - TZ=America/New_York
      - DEBIAN_FRONTEND=noninteractive
    volumes:
      # - <host>:<container>
      - ./hiflow:/home/hiflow3
#volumes:
#  hiflow-host:
# hiflow-host:/home
